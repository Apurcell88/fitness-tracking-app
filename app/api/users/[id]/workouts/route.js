import { connectToDB } from "@/utils/database";
import Workout from "@/models/workout";

export const GET = async (req, { params }) => { // params gets populated if you pass dynamic variables into the url -> params.id
  try {
    
  } catch (err) {
    console.log(err);
  }
}